Fixes applied:
1. Updated frontend Supabase client to use the same Supabase project as Netlify backend env vars.
   This fixes Stripe 'Invalid session' caused by tokens from the wrong Supabase project.
2. Replaced logo assets across source/assets/public/dist.
3. Removed node_modules so this can be uploaded to GitHub/Netlify safely.

Deploy instructions:
- Upload the contents of this project folder to GitHub, not the parent ZIP folder.
- Netlify build settings:
  Base directory: blank, unless this folder is nested in your repo
  Build command: npm run build
  Publish directory: dist
- After GitHub commit, trigger a clear-cache deploy in Netlify.
